{
  "name": "wkfe-standard",
  "version": "0.1.0",
  "description": "wakedata frontend standard",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "prettier": "prettier-config-wk",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "release:lerna": "lerna version --exact --no-git-tag-version",
    "release:only": "lerna exec -- npm publish"
  },
  "devDependencies": {
    "eslint": "^7.8.1",
    "eslint-config-wk": "^0.0.1-alpha.0",
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "prettier": "^2.1.1",
    "prettier-config-wk": "^1.0.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {}
}
